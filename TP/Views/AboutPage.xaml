<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:TP.ViewModels"
             x:Class="TP.Views.AboutPage"
             x:DataType="viewmodels:AboutViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#FAFAFA">
    
    <Grid RowDefinitions="*,Auto">
        <!-- Main Content -->
        <ScrollView Grid.Row="0"
                    BackgroundColor="#FAFAFA">
            <VerticalStackLayout Spacing="25" Padding="20,40,20,20">
                <!-- Title -->
                <Label Text="A Propos"
                       FontSize="28"
                       FontAttributes="Bold"
                       TextColor="Black"
                       HorizontalOptions="Center" />

                <!-- Warning Section -->
                <Border BackgroundColor="#FFF9C4"
                        StrokeThickness="0"
                        Padding="15"
                        StrokeShape="RoundRectangle 10">
                    <Grid ColumnDefinitions="Auto,*">
                        <Border Grid.Column="0"
                                WidthRequest="40"
                                HeightRequest="40"
                                BackgroundColor="#FBC02D"
                                StrokeThickness="2"
                                Stroke="White"
                                StrokeShape="RoundRectangle 20"
                                Margin="0,0,15,0">
                            <Label Text="⚠"
                                   FontSize="20"
                                   TextColor="White"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" Spacing="5">
                            <Label Text="Avertissement"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="Black" />
                            <Label Text="Les informations proviennent de sources officielles."
                                   FontSize="14"
                                   TextColor="Black"
                                   LineBreakMode="WordWrap" />
                            <Label Text="Les règles peuvent changer, vérifiez toujours auprès de votre bureau local."
                                   FontSize="14"
                                   TextColor="Black"
                                   LineBreakMode="WordWrap" />
                        </VerticalStackLayout>
                    </Grid>
                </Border>

                <!-- Contact Section -->
                <VerticalStackLayout Spacing="10">
                    <Label Text="Contact"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="Black" />
                    <Label Text="{Binding Email}"
                           FontSize="16"
                           TextColor="Black" />
                    <Label Text="{Binding Phone}"
                           FontSize="16"
                           TextColor="Black" />
                </VerticalStackLayout>

                <!-- Follow Us Section -->
                <VerticalStackLayout Spacing="15">
                    <Label Text="Suivez Nous"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="Black" />
                    <HorizontalStackLayout Spacing="15">
                        <Border WidthRequest="50"
                                HeightRequest="50"
                                BackgroundColor="White"
                                StrokeThickness="1"
                                Stroke="#E0E0E0"
                                StrokeShape="RoundRectangle 25">
                            <Label Text="f"
                                   FontSize="24"
                                   FontAttributes="Bold"
                                   TextColor="Black"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />
                        </Border>
                        <Border WidthRequest="50"
                                HeightRequest="50"
                                BackgroundColor="White"
                                StrokeThickness="1"
                                Stroke="#E0E0E0"
                                StrokeShape="RoundRectangle 25">
                            <Label Text="🐦"
                                   FontSize="24"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />
                        </Border>
                    </HorizontalStackLayout>
                </VerticalStackLayout>

                <!-- Version -->
                <Label Text="{Binding Version}"
                       FontSize="14"
                       TextColor="#666666"
                       HorizontalOptions="Center"
                       Margin="0,20,0,0" />
            </VerticalStackLayout>
        </ScrollView>

        <!-- Bottom Navigation -->
        <Grid Grid.Row="1"
              BackgroundColor="White"
              Padding="0,10,0,10"
              RowDefinitions="Auto"
              ColumnDefinitions="*,*,*,*">
            <Border Grid.Column="0"
                    BackgroundColor="Transparent"
                    Padding="10">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NavigateToHomeCommand}" />
                </Border.GestureRecognizers>
                <VerticalStackLayout Spacing="5" HorizontalOptions="Center">
                    <Label Text="🏠"
                           FontSize="24"
                           HorizontalOptions="Center" />
                    <Label Text="Accueil"
                           FontSize="10"
                           TextColor="#666666"
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Border>
            
            <Border Grid.Column="1"
                    BackgroundColor="Transparent"
                    Padding="10">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NavigateToExploreCommand}" />
                </Border.GestureRecognizers>
                <VerticalStackLayout Spacing="5" HorizontalOptions="Center">
                    <Label Text="🔍"
                           FontSize="24"
                           HorizontalOptions="Center" />
                    <Label Text="Explorer"
                           FontSize="10"
                           TextColor="#666666"
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Border>
            
            <Border Grid.Column="2"
                    BackgroundColor="Transparent"
                    Padding="10">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NavigateToOfficesCommand}" />
                </Border.GestureRecognizers>
                <VerticalStackLayout Spacing="5" HorizontalOptions="Center">
                    <Label Text="📍"
                           FontSize="24"
                           HorizontalOptions="Center" />
                    <Label Text="Bureaux"
                           FontSize="10"
                           TextColor="#666666"
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Border>
            
            <Border Grid.Column="3"
                    BackgroundColor="Transparent"
                    Padding="10">
                <VerticalStackLayout Spacing="5" HorizontalOptions="Center">
                    <Label Text="ℹ️"
                           FontSize="24"
                           HorizontalOptions="Center" />
                    <Label Text="A Propos"
                           FontSize="10"
                           TextColor="#1A5D4A"
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Border>
        </Grid>
    </Grid>
</ContentPage>

